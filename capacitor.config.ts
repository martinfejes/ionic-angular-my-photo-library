import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-photo-library3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
