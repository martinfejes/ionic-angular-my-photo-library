import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.fejes.photos',
  appName: 'my-photo-library',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
